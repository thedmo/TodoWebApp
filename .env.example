#add a file called .env to the project root and add these variables to it:

POSTGRES_URL=       #your production url
POSTGRES_TEST_URL=  #your url for the test database (only needed if you want to run the testproject)
POSTGRES_USERNAME=  #username
POSTGRES_PASSWORD=  #password

OPENAI_KEY= # OpenAi Api Key for retrieving ai generated categories